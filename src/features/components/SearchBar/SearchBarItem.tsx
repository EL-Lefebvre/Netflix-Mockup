import React from "react";
import { Card, CardContent, CardMedia, Typography } from "@mui/material";
const SearchBarItem = ({ name }) => {
  return <Typography>{name}</Typography>;
};

export default SearchBarItem;
